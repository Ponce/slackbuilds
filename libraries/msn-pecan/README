msn-pecan (an MSN plug-in for libpurple)

It supports pidgin, adium, telepathy-haze, and Nokia N900 (using haze),
and adds "WLM" in the protocol list.  It contains a newer msn protocol
than pidgin's own msn protocol; thus it can be used when pidgin's msn
protocol doesn't work.

