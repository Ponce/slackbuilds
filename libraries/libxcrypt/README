libxcrypt is a modern library for one-way hashing of passwords.

The libxcrypt supports a wide variety of both modern and
historical hashing methods.libxcrypt is intended to be used
by login(1), passwd(1), and other similar programs.
