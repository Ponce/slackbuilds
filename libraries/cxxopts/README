cxxopts is a lightweight, header-only C++ library for parsing command
line arguments, loosely modeled on Python's argparse. It has no
dependencies beyond a C++11 compiler and the standard library.

License: MIT (see /usr/doc/cxxopts-<version>/LICENSE).
