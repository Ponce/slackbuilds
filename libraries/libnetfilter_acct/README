libnetfilter_acct is the userspace library providing interface 
to extended accounting infrastructure.  It is used by nfacct.

You will need to be running a 3.3.x or later kernel to make use
of the netfilter accounting library.
