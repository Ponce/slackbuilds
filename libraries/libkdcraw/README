Libkdcraw is a C++ interface around the dcraw binary program used to decode 
RAW picture files.  This library is used by kipi-plugins, digiKam, and other 
kipi host programs.

This build script uses a svn snapshot that was downloaded and is hosted
by the script maintainer.
