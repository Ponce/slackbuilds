gnome-python-desktop contains a few python modules for GNOME.

This requires gnome-python.

gnome-keyring is required if you want to execute rhythmbox without errors.
evince, gnome-media, metacity, and totem-pl-parser are optional dependencies.
