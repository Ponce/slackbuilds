libasr is asynchronous resolver library which can be used
in place of the resolver functions of libc when blocking is
not desirable.

WARNING! This repository is not updated and
contains known issues that are fixed in upstream OpenBSD.

Please use the bundled version in OpenSMTPD instead.
