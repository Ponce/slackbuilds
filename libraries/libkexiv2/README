Libkexiv2 is a wrapper around Exiv2 library to manipulate picture metadata 
as EXIF/IPTC and XMP.  This library is used by kipi-plugins, digiKam, and 
other kipi host programs.

This build script uses a svn snapshot that was downloaded and hosted by 
the script maintainer.
