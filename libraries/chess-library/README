chess-library is a multi-purpose, header-only C++17 chess library.
It provides chess board representation, legal move generation, PGN
parsing, SAN/UCI helpers, and related utilities in the chess::
namespace.

The upstream project does not currently provide versioned releases, so
this SlackBuild uses a pinned git snapshot.
