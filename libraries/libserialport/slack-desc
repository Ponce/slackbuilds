# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

             |-----handy-ruler------------------------------------------------------|
libserialport: libserialport (cross-platform serial port access library)
libserialport:
libserialport: libserialport is a minimal, cross-platform shared library
libserialport: written in C that is intended to take care of the OS-specific
libserialport: details when writing software that uses serial ports.
libserialport:
libserialport:
libserialport:
libserialport:
libserialport: Homepage: http://sigrok.org/wiki/Libserialport
libserialport:
