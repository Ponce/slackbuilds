QtDMM is a simple DMM readout software including a configurable 
transient recorder.  The transient recorder features manual start, 
scheduled start (at a given time) and triggered automatic start when
given thresholds are reached.
