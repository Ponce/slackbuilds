# No post-install actions are required.  Both binaries are self-contained:
# the font atlas is compiled into wave-render and the X11 client speaks the
# wire protocol, so there is nothing to register, cache or relink.
:
