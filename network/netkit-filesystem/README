This slackbuild packages the Netkit filesystem (part of NetKit available
from SlackBuild.org).

This filesystem contains an installed Debian Linux distribution, including
several general-purpose and network-dedicated packages which are made
available inside Netkit virtual machines.
