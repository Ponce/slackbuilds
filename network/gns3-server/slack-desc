# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

           |-----handy-ruler------------------------------------------------------|
gns3-server: gns3-server (A Network Simulator)
gns3-server:
gns3-server: GNS3 is a graphical network simulator that allows simulation of
gns3-server: complex networks.To allow complete simulations, GNS3 is strongly
gns3-server: linked with:
gns3-server: Dynamips, the core program that allows Cisco IOS emulation.
gns3-server:
gns3-server:
gns3-server:
gns3-server: Homepage: https://www.gns3.com
gns3-server:
