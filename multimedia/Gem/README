Graphics Environment for Multimedia

A pd external (plugin) for OpenGL graphics.

Add to pd settings the startup library 
/usr/lib{64}/Gem/Gem (this is not a typo)

{64} = write 64 if you are in 64bit
