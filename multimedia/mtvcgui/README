mtvcgui is a very simple graphical user interface for TV capture
written in python, using the MPlayer encoder (mencoder) and qt4
libraries.
It allows the user to specify various parameters for the mencoder
command line utility with ad-hoc widgets, and provides sections for
extra parameters.
