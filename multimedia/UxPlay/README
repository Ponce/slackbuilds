You need to start avahi first - /etc/rc.d/rc.avahidaemon

AirPlay your iOS device to linux.

You can try uxplay -fs -s 800x600 -fps 24 for low bandwidth mirroring.

avahi is both build and runtime dependency.
