Seq24 is a minimal loop based midi sequencer.

Seq24 was created to provide a very simple interface for editing and playing
midi 'loops.'

This package requires pangomm, gtkmm, cairomm, libsigc++, and glibmm.
