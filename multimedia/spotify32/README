Spotify is a social music player.

Note: It will not work on CPU's that do not support SSE2. If you have a CPU
that only supports SSE, I recomend you to try the OpenSource libspotify
client.

For the 64-bit client see:
http://slackbuilds.org/repository/14.1/multimedia/spotify64/
