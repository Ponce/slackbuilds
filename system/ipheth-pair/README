The original pairing program shipped with the iPhone USB Ethernet Driver. You
could also use idevicepair instead, which is shipped with latest
libimobiledevice releases.

This package requires libimobiledevice.
