The usbmon(8) utility is a user interface to its namesake "usbmon",
a facility in Linux kernel. It allows monitoring of raw USB traffic in
a manner similar to the way tcpdump monitors network traffic.
