# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

    |-----handy-ruler------------------------------------------------------|
doas: doas (portable privilege elevation utility)
doas:
doas: doas is a sudo alternative originally written for OpenBSD. It allows
doas: a user to run a command as a different user, most often root. doas
doas: offers simple configuration syntax; create a config file at
doas: /etc/doas.conf to get started. See doas.conf(5).
doas:
doas:
doas: Author: Ted Unangst
doas: Port maintainer: Jesse Smith
doas: https://codeberg.org/thejessesmith/doas
