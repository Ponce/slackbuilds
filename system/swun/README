Swun is a very simple Software Update notifier.
It places itself as an an icon into the notification area of 
the desktop environment.  Swun requires a running slackpkg 
configuration (e.g. running slackpkg update).
