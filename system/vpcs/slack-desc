# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

    |-----handy-ruler------------------------------------------------------|
vpcs: vpcs (Virtual PC Simulator)
vpcs:
vpcs: VPCS is an integrated tool in GNS3 that emulates end-host computers
vpcs: with minimal CPU and RAM resource consumption. It allows users to
vpcs: configure basic network functions such as IP addresses, subnet masks,
vpcs: gateways, and DHCP.
vpcs:
vpcs:
vpcs: Its primary purpose is to provide a quick
vpcs: environment for essential connectivity testing using commands like
vpcs: ping and traceroute without the need to run full virtual machines.
