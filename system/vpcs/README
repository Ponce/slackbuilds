VPCS is an integrated tool in GNS3 that emulates end-host computers
with minimal CPU and RAM resource consumption.

It allows users to configure basic network functions such as
IP addresses, subnet masks, gateways, and DHCP.
Its primary purpose is to provide a quick environment for essential
connectivity testing using commands like ping and traceroute without
the need to run full virtual machines.
