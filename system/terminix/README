Terminix is a tiling terminal emulator which uses the VTE GTK+ 3 widget.

After installing please enable the profile script:
chmod +x /etc/profile.d/vte_terminix.sh
Otherwise Terminix will warn you about a configuration error on startup.
