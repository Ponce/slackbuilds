cddfs is a FUSE filesystem that can be used to mount audio CDs. The
tracks will be represented as WAV audio files.

To compile and use this package, the fuse package is also required.
A SlackBuild for fuse is available via SlackBuilds.org.

The cddfs website can be found at: 
http://castet.matthieu.free.fr/cddfs/
