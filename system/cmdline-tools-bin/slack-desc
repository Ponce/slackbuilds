# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                 |-----handy-ruler------------------------------------------------------|
cmdline-tools-bin: cmdline-tools-bin (Android SDK package manager)
cmdline-tools-bin:
cmdline-tools-bin: The sdkmanager is a command-line tool that lets you view, install,
cmdline-tools-bin: update, and uninstall packages for the Android SDK.
cmdline-tools-bin:
cmdline-tools-bin: HOMEPAGE: https://developer.android.com/tools#tools-sdk
cmdline-tools-bin:
cmdline-tools-bin:
cmdline-tools-bin:
cmdline-tools-bin:
cmdline-tools-bin:
