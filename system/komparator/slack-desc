# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.  Line
# up the first '|' above the ':' following the base package name, and the '|'
# on the right side marks the last column you can put a character in.  You must
# make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':'.

          |-----handy-ruler--------------------------------------------------|
komparator: komparator (komparator synchronizes two directories)
komparator: 
komparator: Komparator is a KDE application that searches and synchronizes two 
komparator: directories.  It discovers duplicate, newer, or missing files and 
komparator: empty folders.  It works on local and network / kioslave protocol 
komparator: folders (like smb:/, ftp://, media:/ or fonts:/).
komparator: 
komparator: 
komparator: 
komparator: 
komparator: 
