gdisk is a disk partitioning tool loosely modeled on Linux fdisk, but
used for modifying GUID Partition Table (GPT) disks rather than the
Master Boot Record (MBR) partition tables that fdisk handles.
