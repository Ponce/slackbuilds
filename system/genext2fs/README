genext2fs - generate ext2 filesystem as a normal user

genext2fs generates an ext2 filesystem as a normal (i.e. non-root)
user. It doesn't require you to mount the image file to copy files
on it. It doesn't even require you to be the superuser to make
device nodes or set group/user ids.
