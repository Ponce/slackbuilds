# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

            |-----handy-ruler------------------------------------------------------|
tdb-addition: tdb-addition (A Trivial Database - missing files)
tdb-addition:
tdb-addition: This is a simple database API, similar to GDBM but allows
tdb-addition: simultaneous commits.
tdb-addition: This script packages the missing files in the samba Slackware
tdb-addition: package to have an usable tdb installation.
tdb-addition:
tdb-addition:
tdb-addition:
tdb-addition:
tdb-addition:
