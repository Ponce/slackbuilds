adcfw-log  An iptables (among other things) Log analyser

adcfw-log is a tool for analyzing firewall logs in order to extract
meaningful information. It is designed to generate different kinds of
reports such as fully formatted reports of what had been logged, with
summaries protocol. There are also options to filter the input data by
date, host, protocol, service, and so on.  Only netfilter log format is
supported at this time.

