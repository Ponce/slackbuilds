CATSFC is a Super Nintendo video game system emulator based on Snes9x
1.43 that can be used as a libretro core.

To build with the griffin LTCG use:
  GRIFFIN=1 ./CATSFC-libretro.SlackBuild

To build the debugging symbols use:
  DEBUG=1 ./CATSFC-libretro.SlackBuild
