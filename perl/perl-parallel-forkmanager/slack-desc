# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                         |-----handy-ruler------------------------------------------------------|
perl-parallel-forkmanager: perl-parallel-forkmanager (A simple parallel processing fork manager)
perl-parallel-forkmanager:
perl-parallel-forkmanager: This module is intended for use in operations that can be done in
perl-parallel-forkmanager: parallel where the number of processes to be forked off should be
perl-parallel-forkmanager: limited. Typical use is a downloader which will be retrieving
perl-parallel-forkmanager: hundreds/thousands of files.
perl-parallel-forkmanager:
perl-parallel-forkmanager: HOMEPAGE: https://github.com/dluxhu/perl-parallel-forkmanager
perl-parallel-forkmanager:
perl-parallel-forkmanager:
perl-parallel-forkmanager:
