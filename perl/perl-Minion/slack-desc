# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

           |-----handy-ruler------------------------------------------------------|
perl-Minion: perl-Minion (A high performance job queue for Perl)
perl-Minion:
perl-Minion: It has support for multiple named queues, priorities, high priority
perl-Minion: fast lane, delayed jobs, job dependencies, job progress, job
perl-Minion: results, retries with backoff, rate limiting, unique jobs, expiring
perl-Minion: jobs, statistics, distributed workers, parallel processing,
perl-Minion: autoscaling, remote control, Mojolicious admin ui, resource leak
perl-Minion: protection and multiple backends (such as Postgres.) Needs at least
perl-Minion: one backend, which defaults to Mojo::Pg, but there are other backends
perl-Minion: avalable, such as Minion::Backend::SQLite, and Minion::Backend::mysql.
perl-Minion:
