# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                   |-----handy-ruler------------------------------------------------------|
perl-HTML-Formatter: perl-HTML-Formatter (Base class for HTML formatters)
perl-HTML-Formatter:
perl-HTML-Formatter: HTML::Formatter is a base class for classes that take HTML and format
perl-HTML-Formatter: it to some output format.
perl-HTML-Formatter:
perl-HTML-Formatter: Homepage: https://metacpan.org/pod/HTML::Formatter
perl-HTML-Formatter:
perl-HTML-Formatter:
perl-HTML-Formatter:
perl-HTML-Formatter:
perl-HTML-Formatter:
