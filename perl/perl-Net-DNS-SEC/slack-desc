# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                |-----handy-ruler------------------------------------------------------|
perl-Net-DNS-SEC: perl-Net-DNS-SEC (DNSSEC extensions to Net::DNS)
perl-Net-DNS-SEC:
perl-Net-DNS-SEC: DNSSEC extension to Net::DNS.  It implements the cryptographic
perl-Net-DNS-SEC: signature generation and verification functions that are relevant for
perl-Net-DNS-SEC: DNSSEC operations.
perl-Net-DNS-SEC:
perl-Net-DNS-SEC: Homepage: https://metacpan.org/pod/Net::DNS::SEC
perl-Net-DNS-SEC:
perl-Net-DNS-SEC:
perl-Net-DNS-SEC:
perl-Net-DNS-SEC:
