perl-CPAN-Meta-YAML (Read and write a perl-CPAN-Meta-YAML: subset of YAML for CPAN Meta files)

This module implements a subset of the YAML specification for use in reading and writing 
CPAN metadata files like META.yml and MYMETA.yml.
It should not be used for any other general YAML parsing or generation task.
