perl-io-uncompress-brotli (brotli support for perl)

Brotli is a data compression format primarily used in web fonts and
(more recently) HTTP compression. This package contains modules for
compressing Brotli buffers and streams (IO::Compress::Brotli), and
decompressing (IO::Uncompress::Brotli).
