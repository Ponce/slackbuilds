xcur2png (convert idiotic Xcursor format files to PNG)

xcur2png is a program which lets you take PNG images from X cursors,
and generate a config file which is reusable by xcursorgen. To put it
simply, it converts from the X cursor format to PNG images.
