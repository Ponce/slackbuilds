Gimmage is a small gtk image viewer, perfect for command line usage
as it accepts directories and image filenames as arguments. It also
has a filechooser integrated into the main UI, making accessing images 
and directories a snap.

This requires gtkmm (available at SlackBuilds.org).
