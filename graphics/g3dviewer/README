G3D-Viewer is a application which employs the OpenGL and GtkGLExt
libraries to render a three-dimensional model in a graphical window.

There is also a feature for rendering a thumbnail for use in a
graphical file manager.

A number of different input file formats are supported.
