Graphical SQL report writer, designer and rendering engine,
optimized for PostgreSQL. WYSIWYG display, GUI built with Qt.
Server-side rendering engine. Reports can be saved as XML,
either as files or in a database.

Requires qt4, which is also available from SlackBuilds.org.