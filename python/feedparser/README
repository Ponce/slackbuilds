feedparser is a Python utility library to parse RSS and Atom feeds.

The feedparser version is frozen at 6.0.12 for Slackware 15.0 due
to upstream dropping support for Python 3.9 in later versions.

This SlackBuild supports only Python 3
