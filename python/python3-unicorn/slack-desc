# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

               |-----handy-ruler------------------------------------------------------|
python3-unicorn: python3-unicorn (Python bindings for the Unicorn CPU emulator)
python3-unicorn:
python3-unicorn: Python bindings for Unicorn, a lightweight multi-platform,
python3-unicorn: multi-architecture CPU emulator framework based on QEMU.
python3-unicorn:
python3-unicorn: This package requires the unicorn native library.
python3-unicorn:
python3-unicorn:
python3-unicorn:
python3-unicorn: Homepage: https://www.unicorn-engine.org/
python3-unicorn:
