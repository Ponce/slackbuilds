# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                          |-----handy-ruler------------------------------------------------------|
python3-icalendar-searcher: python3-icalendar-searcher (Filter sort & expand icalendar components)
python3-icalendar-searcher:
python3-icalendar-searcher: This library should contain logic for searching, filtering and sorting
python3-icalendar-searcher: icalendar data, as well as logic for storing and representing an
python3-icalendar-searcher: icalendar search query.
python3-icalendar-searcher:
python3-icalendar-searcher: HOMEPAGE: https://github.com/pycalendar/icalendar-searcher
python3-icalendar-searcher:
python3-icalendar-searcher:
python3-icalendar-searcher:
python3-icalendar-searcher:
