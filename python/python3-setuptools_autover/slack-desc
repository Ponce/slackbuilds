# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                          |-----handy-ruler------------------------------------------------------|
python3-setuptools_autover: python3-setuptools_autover (version number generator for setuptools)
python3-setuptools_autover:
python3-setuptools_autover: A package allowing generating PEP-440 compatible version numbers from
python3-setuptools_autover: version control.  Currently only git is supported.  Provides
python3-setuptools_autover: reasonable defaults and an easy way to customize how version numbers
python3-setuptools_autover: are generated from version control system.
python3-setuptools_autover:
python3-setuptools_autover:
python3-setuptools_autover:
python3-setuptools_autover:
python3-setuptools_autover:
