# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                |-----handy-ruler------------------------------------------------------|
python3-imaplib2: python3-imaplib2 (a threaded Python IMAP4 client)
python3-imaplib2:
python3-imaplib2:
python3-imaplib2:
python3-imaplib2: Based on RFC 3501 and original imaplib module.
python3-imaplib2:
python3-imaplib2: This is a version of imaplib that uses threads to allow full use of
python3-imaplib2: the IMAP4 concurrency features, and to de-couple a user of imaplib
python3-imaplib2: from i/o lags, except where explicitly allowed.
python3-imaplib2:
python3-imaplib2:
