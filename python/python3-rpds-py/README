python3-rpds-py (Python bindings to Rust's pers. data struc. "rpds")

Python bindings to the Rust rpds crate for persistent data structures.

#NOTE: 0.27.1 last python3.9 version, but 0.30.0 still works.
