obbrowser (Recursively browse the filesystem through openbox3 pipe menus, with icons.)

To use integrated on obmenu-generator menu scheme (also available on SlackBuilds)
or default OpenBox menu scheme, otherwise it will be fairly useless.
