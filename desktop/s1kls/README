s1kls (Simple X11 1-key Keyboard layout switcher)

Very simple X11 keyboard layout switcher that does not conflict with
application hotkeys. Only LockGroup switch type supported (each key
selects its own layout).
