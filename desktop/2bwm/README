A fast floating WM, with the particularity of having 2 borders, written
over the XCB library and derived from mcwm written by Michael Cardell.

Run getconfig.sh to extract config.h file to the current directory.
Modify it as needed.

Optional dependencies (as mentioned in the default config.h file, unless
you modify it of course):
  * rxvt-unicode
  * xdotool
  * light
