fbmenugen (A simple menu generator for the Fluxbox Window Manager).

fbmenugen generates a valid Fluxbox menu file with the applications
from the /usr/share/applications directory, ordered on categories.

It also provides a configuration file that allows the user to customize the menu.

To generate a simple menu without icons run: fbmenugen.

To generate a menu with icons run: fbmenugen -i.
