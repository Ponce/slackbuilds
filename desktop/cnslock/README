cnslock is a dock application (dockapp) which provides a
visual indication of the states of the three "lock" buttons
(caps, num, and scroll).

Now it's possible to see the state of those keys on keyboards
with no leds on it (wireless).
