xsession-xinitrc (xinitrc integration for display managers)

xsession desktop file and helper to start a session using
xinitrc. it can be used to launch classic window managers
from a display manager like SDDM.

after installation most display managers should offer to
start a session using "xinitrc".
