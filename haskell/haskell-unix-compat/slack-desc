# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                   |-----handy-ruler------------------------------------------------------|
haskell-unix-compat: haskell-unix-compat (Portable POSIX-compatibility layer)
haskell-unix-compat:
haskell-unix-compat: This package provides portable implementations of parts of the unix
haskell-unix-compat: package. This package re-exports the unix package when available.
haskell-unix-compat: When it isn't available, portable implementations are used.
haskell-unix-compat:
haskell-unix-compat:
haskell-unix-compat:
haskell-unix-compat:
haskell-unix-compat:
haskell-unix-compat:
