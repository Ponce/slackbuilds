haskell-text-metrics (Calculate various string metrics efficiently)

The library provides efficient implementations of various string
metric algorithms. It works with strict Text values.

After uninstalling, run this command to unregister the package from
the ghc package database:   ghc-pkg recache
