GNU's source highlight tool
  
GNU Source-highlight, given a source file, produces a document with 
syntax highlighting. It comes with a lot of example css files

It depends on the BOOST C++ Library ( www.boost.org ), for which 
the build script is available at slackbuilds.org

