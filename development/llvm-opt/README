llvm-opt (LLVM compiler toolkit)

The LLVM Project is a collection of modular and reusable compiler and
toolchain technologies.

This SlackBuild includes Clang (a C/C++/Objective-C compiler), LLD (a
linker), LLDB (a debugger), and the LLVM infrastructure.

To use the llvm-opt in a SlackBuild, see README.SLACKWARE
