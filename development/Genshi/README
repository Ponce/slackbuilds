Genshi is a Python toolkit for stream-based generation of output for the web.
Genshi provides an XML-based template engine, a text-based template engine,
and others tools for working with markup.

An optional dependency is Python setuptools (pysetuptools on SlackBuilds.org).
