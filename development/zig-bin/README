zig-bin is the precompiled-binary version of Zig, a general-purpose
programming language and toolchain for maintaining robust, optimal, and
reusable software.

This package installs the official precompiled Zig 0.9.x binaries
provided by upstream. It does not compile Zig from source.

Use the zig package to build Zig from source using the bootstrap
compiler.

NOTE: This script is kept at 0.9.x for river.
If you need newer version of zig, use zig.
