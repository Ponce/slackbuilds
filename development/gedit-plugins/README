gedit-plugins is a collection of extra plugins for use with gedit.

This requires gedit. pygtksourceview is an optional dependency that also needs
to be installed when building gedit in order to support all of the plugins.
