Cairo (perl module)

Perl bindings to the cairo graphics library. 

In order to build it from source, you'll also need
  ExtUtils::PkgConfig >= 0.2 (perl-extutils-pkgconfig)
  ExtUtils::Depends   >= 1.0 (perl-extutils-depends)
  Test::Number::Delta >= 1.0 (perl-test-number-delta)
All of these are also available from SlackBuilds.org.
