rev-plugins (LADSPA reverb plugin)

This is a stereo reverb plugin based on the well-known greverb.

As of v0.7.1, the g2reverb source was split into its own separate
source package. This build includes both zita-reverbs and g2reverb,
for compatibility with older versions.
