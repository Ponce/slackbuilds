# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.  Line
# up the first '|' above the ':' following the base package name, and the '|'
# on the right side marks the last column you can put a character in.  You must
# make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

       |-----handy-ruler------------------------------------------------------|
paprefs: paprefs (PulseAudio Preferences)
paprefs:
paprefs: paprefs is a simple GTK based configuration dialog for the PulseAudio
paprefs: sound server. this program can only configure local servers, and
paprefs: require a special module "module-gconf" is loaded in the sound
paprefs: server.
paprefs: 
paprefs: Website : http://0pointer.de/lennart/projects/paprefs/
paprefs: 
paprefs: 
paprefs:
