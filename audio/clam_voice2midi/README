voice2midi (convert singing/humming into MIDI files)

voice2midi is a GUI tool to that loads audio files containing monophonic
melodies and extracts the melody as a MIDI file or as an XML file.

This requires clam.
