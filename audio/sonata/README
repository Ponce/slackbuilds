Sonata is an elegant GTK+2 based MPD client.

As of the 1.5.x series, Sonata requires python-mpd as a runtime
dependency (however, not necessary to build) - python-mpd is also
available at slackbuilds.org.
