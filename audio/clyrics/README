menutray (An extensible lyrics fetcher, with daemon support for cmus and mocp.)

Main features include:
- support for plugins
- cmus and mocp daemon mode

Copy folder /etc/xdg/clyrics/plugins to your $HOME/.config/clyrics folder.

Open mocp or cmus and run:
$ clyrics -m -P ~/.config/clyrics/plugins/
